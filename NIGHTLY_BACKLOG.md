# NIGHTLY BACKLOG — 夜間自動開発キュー

このファイルは「夜間ナイトリー・エージェント」専用の指示書兼タスクキューです。
ユーザー(koya800)が寝ている間、毎晩1回エージェントが起動し、このキューの
**最上位の未完タスク1つだけ**を `nightly` ブランチで実装します。

---

## エージェント実行手順（毎晩これに従う）

1. `nightly` ブランチをチェックアウト（無ければ `main` から作成）。`main` には**絶対に触らない**。
2. このファイルの「タスクキュー」で `[ ]`（未完）の**最上位1件のみ**を選ぶ。
3. そのタスクを実装する。スコープを超える追加機能・大規模リファクタはしない。
4. **構文チェック必須**（`ball_pit_survivor.html` 編集後）:
   - 末尾の `</script>` `</body>` `</html>` を消していないか `tail -3` で確認。切れていたら書き戻す。
   - JS抽出 → `node --check`:
     ```
     python3 -c "import re; html=open('ball_pit_survivor.html').read(); js=re.search(r'<script>(.*?)</script>',html,re.DOTALL).group(1); open('/tmp/t.js','w').write(js)"
     node --check /tmp/t.js
     ```
   - エラーが出たら修正し、直らなければそのタスクを `[ ]` のままにして `NIGHTLY_LOG.md` に失敗理由を記録し中断（壊れたコードを commit しない）。
5. `sw.js` の `CACHE_NAME` を1つ bump（例 `bps-v8` → `bps-v9`）。PWA更新がスマホに届くために必須。
6. 完了タスクの `[ ]` を `[x]` にし、日付と一言を付ける。
7. `nightly` ブランチにのみ commit & push。コミットメッセージ末尾に
   `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`。
8. `NIGHTLY_LOG.md` に当夜エントリを追記（後述フォーマット）。

## 禁止事項（厳守）

- `main` への push / マージ / PR 作成（GitHub Pages が main 配信＝即公開。ユーザーが実機テスト中の本番を壊さない）
- AAB/APK 生成、PWABuilder、Netlify、Google Play Console の操作・公開準備の催促
- **ゲームバランス数値の自動変更**（敵スケーリング/ショップコスト/スポーン頻度/HP等）。
  気づいた改善案は実装せず `NIGHTLY_LOG.md` の「バランス提案」に文章で書くだけ。
  理由: 現在テスト段階で、バランスの良し悪しはユーザーが実機で体感して決める領域。
- 1晩で複数タスクの同時投入（diff をレビュー可能な粒度に保つため）

## 設計前提（既存資産を再利用すること）

- 単一HTMLファイル完結・外部依存なし・Canvas描画・Web Audio合成。新規ライブラリ追加禁止。
- 永続化は localStorage の既存 `meta_key` 機構（装備/コイン/8種永続強化/選択ステージ/BGM ON/OFF）を流用。
- 武器・パッシブ・敵は既存の配列/定義パターンに沿って追加する。

---

## タスクキュー（上から消化・1晩1件）

- [ ] **1. STAGES の UI 統合**
  コードに `STAGES` 定義済みだがタイトル画面に未接続。タイトル画面にステージ選択UI
  （背景＋難易度モード）を配線し、選択を既存 localStorage 永続化に保存、ゲーム開始時に反映。
  受入: タイトルで複数ステージを選べ、再起動後も選択が保持され、選択により背景/難易度が変わる。

- [ ] **2. 新武器: チェーンライティング（連鎖雷）**
  既存武器11種のパターンに沿って追加。命中した敵から近接敵へ連鎖する挙動。Lv1〜8の
  `lvlText` を既存形式で記述。進化形(Lv5+特定パッシブ)も既存進化の枠組みで1つ用意。
  受入: レベルアップ選択肢に出現し、取得すると連鎖雷が発射・連鎖し、Lvで強化される。

- [ ] **3. 新武器: サイクロン（旋風）**
  プレイヤー周囲を回転する持続ダメージ系。既存武器パターンに沿って追加、Lv1〜8 `lvlText`、
  進化形1つ。
  受入: 取得でプレイヤー周囲に旋風が出現し接触ダメージ、Lvで範囲/威力上昇。

- [ ] **4. 実績システム**
  localStorage `meta_key` に実績達成フラグを保存。達成時に画面トースト表示。
  初期実績例: 初クリア / 5分到達 / 累計コイン1万 / 武器1種をLv8 / ボス10体撃破。
  受入: 条件達成でトーストが出て、再起動後も達成済みが保持・一覧表示できる。

- [ ] **5. デイリーチャレンジ**
  日付シードで日替わりの特別ルール/目標を1つ生成（例: 特定武器縛り＋目標タイム）。
  達成状況を localStorage に日付キーで保存。リテンション向上が目的。
  受入: 同じ日は同じチャレンジ、翌日は変わる、達成記録が残る。

- [ ] **6. 真のラスボス（300秒時点）**
  既存ボスウェーブ機構を拡張し、CLEAR_TIME(300秒)到達時に専用ラスボスを出現。
  撃破で「真クリア」演出。撃破せず時間経過の通常クリアとは区別。
  受入: 5分到達でラスボス出現、撃破で真クリア表示、未撃破時は従来通常クリア。

- [ ] **7. 新敵タイプ用SFX追加**
  既存 Web Audio 合成パターンに沿い、SFXが薄い敵アクション（合体/ボス出現等）へ
  控えめな効果音を追加。音量は既存BGM/SFXバランスを壊さない範囲。
  受入: 該当イベントで新SFXが鳴り、BGM ON/OFF設定に従う。

- [ ] **8. 画面遷移アニメーション**
  タイトル↔ゲーム↔リザルトの遷移にフェード等の軽い演出を追加（パフォーマンス劣化なし）。
  受入: 遷移が滑らかになり、低速端末でもフレーム落ちしない。

> キューが空になったら新規実装は止め、`NIGHTLY_LOG.md` に「バックログ枯渇。バランス提案
> とバグ観察のみ記録」と書き、その夜はコード変更せず観察ノートだけ追記すること。
